spring:
  application:
    name: balance
  datasource:
    url: jdbc:postgresql://localhost:5433/balance
    username: postgres_balance
    password: password_postgres_balance

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect