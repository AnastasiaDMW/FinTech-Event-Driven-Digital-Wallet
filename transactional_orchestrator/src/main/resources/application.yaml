spring:
  application:
    name: transactional_orchestrator
  datasource:
    url: jdbc:postgresql://localhost:5432/transactional_orchestrator
    username: postgres_transactional_orchestrator
    password: password_postgres_transactional_orchestrator
  
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect