{
  "name": "outbox-connector",
  "config": {
    "connector.class": "io.debezium.connector.postgresql.PostgresConnector",
    "database.hostname": "postgres_transactional_orchestrator",
    "database.port": "5432",
    "database.user": "debezium",
    "database.password": "dbz",
    "database.dbname": "transactional_orchestrator",
    "database.server.name": "transactional-orchestrator",
    "publication.name": "outbox_pub",
    "publication.autocreate.mode": "disabled",
    "slot.name": "debezium_kafka_outbox_slot",
    "table.include.list": "public.tbl_outbox",
    "plugin.name": "pgoutput",
    "topic.prefix": "topic",
    "transforms": "outbox,headerize",
    "key.converter": "org.apache.kafka.connect.storage.StringConverter",
    "value.converter": "org.apache.kafka.connect.json.JsonConverter",
    "value.converter.schemas.enable": "false",
    "transforms.outbox.type": "io.debezium.transforms.outbox.EventRouter",
    "transforms.outbox.table.field.event.id": "id",
    "transforms.outbox.table.field.event.key": "event_key",
    "transforms.outbox.table.field.event.type": "event_type",
    "transforms.outbox.table.field.event.payload": "payload",
    "transforms.outbox.route.by.field": "category",
    "transforms.outbox.route.topic.replacement": "${routedByValue}",
    "transforms.outbox.remove.table.fields": "true",
    "transforms.headerize.type": "org.apache.kafka.connect.transforms.HeaderFrom$Value",
    "transforms.headerize.headers": "event_type",
    "transforms.headerize.fields": "event_type",
    "transforms.headerize.operation": "move"
  }
}