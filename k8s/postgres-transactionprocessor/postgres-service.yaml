apiVersion: v1
kind: Service
metadata:
  name: postgres-transactionprocessor-service
  namespace: microservices
spec:
  selector:
    app: postgres-transactionprocessor
  ports:
  - port: 5432
    targetPort: 5432
  type: ClusterIP