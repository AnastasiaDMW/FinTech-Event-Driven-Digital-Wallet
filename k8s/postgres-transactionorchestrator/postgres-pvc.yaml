apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-transactionorchestrator-pvc
  namespace: microservices
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi