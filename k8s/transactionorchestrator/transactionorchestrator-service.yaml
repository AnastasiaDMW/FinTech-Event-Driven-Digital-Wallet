apiVersion: v1
kind: Service
metadata:
  name: transactional-orchestrator
  namespace: microservices
spec:
  selector:
    app: transactional-orchestrator
  ports:
  - port: 8080
    targetPort: 8080
  type: ClusterIP