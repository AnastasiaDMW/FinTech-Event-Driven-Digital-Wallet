apiVersion: v1
kind: Service
metadata:
  name: postgres-account-service
  namespace: microservices
spec:
  selector:
    app: postgres-account
  ports:
  - port: 5432
    targetPort: 5432
  type: ClusterIP