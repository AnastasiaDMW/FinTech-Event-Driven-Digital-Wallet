apiVersion: v1
kind: Service
metadata:
  name: mongodb-service
  namespace: microservices

spec:
  selector:
    app: mongodb

  ports:
    - name: mongodb-service
      port: 27017
      targetPort: 27017

  type: ClusterIP