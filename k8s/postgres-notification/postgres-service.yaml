apiVersion: v1
kind: Service
metadata:
  name: postgres-notification-service
  namespace: microservices
spec:
  selector:
    app: postgres-notification
  ports:
  - port: 5432
    targetPort: 5432
  type: ClusterIP