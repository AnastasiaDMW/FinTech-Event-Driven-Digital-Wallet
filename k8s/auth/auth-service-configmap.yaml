apiVersion: v1
kind: ConfigMap
metadata:
  name: auth-service-config
  namespace: microservices
data:
  authservice.toml: |
    bind_addr=":8081"
    log_level="debug"
    log_format="text"
    
    # Используем пользователя auth из Redis ACL
    redis_addr="redis-service:6379"
    redis_username="auth"
    redis_password="Au1th$ecretP@sz163"
    
    [store]
    database_url="postgres://postgres:1234@postgres-auth-service:5432/auth_service_dev?sslmode=disable"