apiVersion: v1
kind: Service
metadata:
  name: postgres-balance-service
  namespace: microservices
spec:
  selector:
    app: postgres-balance
  ports:
  - port: 5432
    targetPort: 5432
  type: ClusterIP